import { SelectionParameters } from "@/types/parameters";

export const SELECTION_DEFAULT: SelectionParameters = {
    lineWidth: 2,
    color: "#ff0000",
    zoom: 1,
    isPanning: false,
    isDrawing: true,
};